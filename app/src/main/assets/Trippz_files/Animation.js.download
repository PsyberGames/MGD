class Animator{
    //animator held in gameobject if gameobject has animation
constructor(imgSrc, TotalCells,imgWidth,imgHeight,frames,spritewidth,spriteheight) {
    this.cellCount = [];
    this.cellCount.length = TotalCells;
    this.spriteSheet = imgSrc;
    this.width = imgWidth;
    this.height = imgHeight;
    this.totalFrames = frames;
    this.spriteWidth = spritewidth;
    this.spriteHeight = spriteheight;
    this.startCell = 1;
    this.rowCellCount = this.width / this.totalFrames;
}
    Update(){


    }
}